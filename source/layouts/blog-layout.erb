
<% wrap_layout :layout do %>

      <div class='date'><%= current_page.date.strftime('%b %d | %Y') %></div>

      <article class="index-article">

        <h2 class='post-title'><%= title %></h2>

    <% if current_page.data.contentful_pics %>
    <div class='slick-container'>  
      <div class="carousel">
      <% current_page.data.contentful_pics.each do |pic_id| %>
        <figure class="image-block__figure"><img src="//images.contentful.com/<%= pic_id %>" sizes="(min-width: 1160px) 1160px, 96vw" alt="" class="image-block__figure-image">
        </figure>
      <% end %>
      </div>
    </div>
    <% end %>

    <% if current_page.data.vimeo_vids %>
    <!---<h3 id='other-episodes'>Interviews</h3> -->
    <% current_page.data.vimeo_vids.each do |vid_id| %>
    <div class='vimeo-player vimeo-spacing'> 
      <iframe src="https://player.vimeo.com/video/<%= vid_id %>?color=ababab&portrait=0"
        width="640"
        height="360"
        frameborder="0"
        webkitallowfullscreen
        mozallowfullscreen
        allowfullscreen>
      </iframe>
    </div>
    <% end %>
    <% end %>

    <% if current_page.data.vimeo_id %>
    <!---<h3 id='other-episodes'>Interviews</h3> -->
    <div class='vimeo-player'>
      <iframe src="https://player.vimeo.com/video/<%= current_page.data.vimeo_id %>?color=ababab&portrait=0"
        width="950"
        height="534"
        frameborder="0"
        webkitallowfullscreen
        mozallowfullscreen
        allowfullscreen>
      </iframe>
    </div>
    <% end %>

    <% if soundcloud_id %>
    <section class='soundcloud-player-big'>
      <iframe 
        width="100%" 
        height="450" 
        scrolling="no" 
        frameborder="no" 
        src=<%= big_soundcloud_player %>>
      </iframe>
    </section>
    <% end %>

    <% if topic_list %>
    <h3 class='topic-list'><%= topic_list %></h3>
    <% end %>

    <!--- <% if interviewee %>
      <h3 id='other-episodes'>Other Episodes</h3>
      <a class='other-episodes' href="/tags/<%= dasherize(interviewee) %>"><%= interviewee %></a>
    <% end %> -->

    <%= current_article.body %>

  </article>

<% end %>
